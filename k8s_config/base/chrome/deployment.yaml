apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    name: chrome
  name: chrome
spec:
  selector:
    matchLabels:
      app: chrome
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: chrome
    spec:
      containers:
        - image: browserless/chrome:latest
          name: chrome
          ports:
            - name: chrome
              containerPort: 3000
          env:
            - name: HOST
              value: "0.0.0.0"
            - name: DEBUG
              value: "browserless/chrome*"
            - name: EXIT_ON_HEALTH_FAILURE
              value: "true"
            - name: ENABLE_DEBUGGER
              value: "false"
            - name: MAX_CONCURRENT_SESSIONS
              valueFrom:
                configMapKeyRef:
                  key: MAX_CONCURRENT_SESSIONS
                  name: env-vars
            - name: PREBOOT_CHROME
              value: "true"
            # - name: CONNECTION_TIMEOUT
            #   value: "900000"
        # - image: gcr.io/bitpull/pressure:latest
        #   name: pressure
        #   env:
        #     - name: PUPPETEER_URL
        #       value: "http://localhost:3000"
        #     - name: CHECK_INTERVAL
        #       value: "1"
        #     - name: MAXIMUM_QUEUE_SIZE
        #       valueFrom:
        #         configMapKeyRef:
        #           key: MAX_CONCURRENT_SESSIONS
        #           name: env-vars
           