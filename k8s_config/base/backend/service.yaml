apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  ports:
    - name: http
      port: 4000
      targetPort: 5000
      protocol: TCP
  selector:
    app: backend
    tier: backend
  type: NodePort
